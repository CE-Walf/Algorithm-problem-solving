SELECT
    SUM(SCORE) AS SCORE,
    EMP_NO,
    EMP_NAME,
    POSITION,
    EMAIL
FROM
    HR_DEPARTMENT
INNER JOIN
    HR_EMPLOYEES
USING
    (DEPT_ID)
INNER JOIN
    HR_GRADE
USING
    (EMP_NO)
WHERE
    YEAR = '2022'
GROUP BY
    EMP_NO
ORDER BY
    SCORE DESC
LIMIT 1;
    

    